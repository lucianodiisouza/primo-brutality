# @primo-brutality/ui

## 2.0.0

### Major Changes

- e32e6a0: Remove the `ScrollView` export. Use React Native's `ScrollView`, `FlatList`, or `@shopify/flash-list` directly in app code instead.

## 1.0.0

### Major Changes

- Drop the `Brut` prefix from all component and type exports. Import `Button`, `Text`, `Card`, etc. directly from `@primo-brutality/ui`.

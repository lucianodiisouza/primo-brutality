import { Meta, Canvas, Controls, Primary, Stories } from "@storybook/addon-docs/blocks";
import * as BrutBadgeStories from "./BrutBadge.stories";

<Meta of={BrutBadgeStories} />

# BrutBadge

Compact status pill for labels like "New", "Beta", or category tags.

## Usage

```tsx
import { BrutBadge } from "@primo-brutality/ui";

<BrutBadge label="New" variant="pink" />
```

<Primary />

<Controls />

<Canvas of={BrutBadgeStories.AllVariants} />

<Stories />

import { Meta, Canvas, Stories } from "@storybook/addon-docs/blocks";
import * as BrutPinInputStories from "./BrutPinInput.stories";

<Meta of={BrutPinInputStories} />

# BrutPinInput

OTP / PIN entry with visible digit cells and a hidden numeric input.

## Usage

```tsx
import { BrutPinInput } from "@primo-brutality/ui";

const [code, setCode] = useState("");

<BrutPinInput
  length={6}
  value={code}
  onChange={setCode}
  onComplete={(pin) => verify(pin)}
/>
```

<Canvas of={BrutPinInputStories.SixDigit} />

<Stories />
